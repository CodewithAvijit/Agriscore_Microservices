services:
  - type: web
    name: agri-fastapi
    env: python
    pythonVersion: 3.10.11
    branch: ml
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn "crop_recommendation.app.main:app" --host=0.0.0.0 --port=10000
    envVars:
      - key: PORT
        value: 10000
